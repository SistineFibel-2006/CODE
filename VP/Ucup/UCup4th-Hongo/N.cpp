#include <bits/stdc++.h>
#include <queue>
using namespace std;
#define fi first
#define se second 
#define ll long long



#define int long long

void solve() {
    
    int n;
    cin >> n;
    vector<int>arr(n, 0);
    
    for(auto &x : arr){
      cin >> x;
    }

    sort(arr.begin(), arr.end());

    priority_queue<array<int,3>>save;

    
 
} 

signed main() {
  int t = 1;
//   cin >> t;
  while(t --) solve();
}