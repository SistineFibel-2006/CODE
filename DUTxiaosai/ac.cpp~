#include <bits/stdc++.h>
using namespace std;

int main() {
    int a; cin >> a;
    set<int> ss;
    for(int i = 0; i < a; i ++) {
	int x; cin >> x;
	ss.insert(x);
    }
    if(ss.size() == a) cout << "Yes";
    else cout << "No";
}
