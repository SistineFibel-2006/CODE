#include <bits/stdc++.h>
using namespace std;

using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
using u128 = unsigned __int128;

//超级板子（无 std前缀 ， endl = '\n' , 但是用真正endl可加std::）

#define endl '\n'

#define fi first
#define se second

signed main (){
	std::ios::sync_with_stdio(false);std::cin.tie(0);
	int n;
	cin >> n;
	vector<int > a(n,-1);
	vector<int > b(n,-1);
	for(int i = 0; i < n; i ++)
		cin >> a[i] >> b[i];



	return 0;
}